//import
import { reactive, watch, onMounted, ref } from "vue"
import datas from "../../../public/mock/data.json"
//start
export default {
  name: 'main',
  components: {

  },
  props: {

  },
  setup() {
    let state = reactive({})
    onMounted(async () => {
    });
    
    return {
    }
  }
}